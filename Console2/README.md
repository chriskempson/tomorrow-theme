To install, close all instances of Console2, then edit your `console.xml` (either local or system-wide, depending on your setup) and replace the `<color>` tags with the those from this folder's `console.xml`.
